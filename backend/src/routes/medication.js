const express = require('express');
const router = express.Router();

// Example route (replace with real implementation)
router.get('/', (req, res) => {
  res.json({ message: 'Medication route working!' });
});

module.exports = router;



